{
  "name": "docket",
  "version": "0.1.0",
  "private": true,
  "description": "Docket slice 0 — database, security and provider adapters. The Next.js app arrives in slice 1 (see BUILD_PROMPTS.md).",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "db:test:local": "bash scripts/db-test-local.sh"
  },
  "devDependencies": {
    "@types/node": "^22.5.0",
    "typescript": "^5.6.0"
  }
}
